# Комментарии в начале файла
# Это комментарий для документации

# Простые переменные
DATABASE_NAME=myapp_db
PORT=3000
DEBUG=true

# Переменные с пробелами (должны игнорироваться вокруг знака =)
SPACED_VAR = value_with_spaces
  INDENTED_VAR=indented_value

# Пустые строки должны игнорироваться


# Переменные в кавычках
SINGLE_QUOTED='single quoted value'
DOUBLE_QUOTED="double quoted value"
MIXED_QUOTES='value with "inner" quotes'

# Переменные с экранированием
ESCAPED_QUOTES="value with \"escaped\" quotes"
ESCAPED_NEWLINE="line1\nline2"
ESCAPED_TAB="value\twith\ttabs"

# Многострочные значения
MULTILINE_SINGLE='line1
line2
line3'
MULTILINE_DOUBLE="line1
line2
line3"

# Переменные с особыми символами
SPECIAL_CHARS="!@#$%^&*()_+-=[]{}|;:,.<>?"
URL_VALUE="https://example.com:8080/path?param=value&other=123"
EMAIL_VALUE=user@example.com

# Переменные с пустыми значениями
EMPTY_VALUE=
EMPTY_QUOTED=""
EMPTY_SINGLE=''

# Переменные только с пробелами
WHITESPACE_ONLY="   "
TAB_ONLY="		"

# Переменные с интерполяцией (зависит от парсера)
HOME_DIR=/home/user
LOG_FILE=${HOME_DIR}/logs/app.log
BACKUP_PATH=$HOME_DIR/backups

# Булевые значения
IS_PRODUCTION=false
ENABLE_CACHE=TRUE
SHOW_DEBUG=0
SEND_EMAILS=1

# Числовые значения
MAX_CONNECTIONS=100
TIMEOUT_SECONDS=30.5
NEGATIVE_NUMBER=-42

# Переменные с подчеркиваниями и цифрами
API_KEY_V2=abc123def456
USER_ID_123=user_value
_PRIVATE_VAR=private_value

# Длинные значения
LONG_TEXT="Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris."

# Переменные с хешами и токенами
JWT_SECRET=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9
API_TOKEN=sk-1234567890abcdefghijklmnopqrstuvwxyz
HASH_VALUE=a1b2c3d4e5f6789012345678901234567890abcd

# Комментарии после значений (некоторые парсеры не поддерживают)
INLINE_COMMENT=value # это комментарий
INLINE_COMMENT_SINGLE_QUOTED='value' # это комментарий
INLINE_COMMENT_DOUBLE_QUOTED="value" # это комментарий
ANOTHER_VALUE=test#notcomment

# Переменные с экзотическими именами (могут не поддерживаться всеми парсерами)
CAMELCASE=camelValue
kebab-case=kebabValue
dot.notation=dotValue

# Значения, содержащие знак равенства
EQUATION="x=y+z"
BASE64_VALUE="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg=="

# Переменные с кириллицей (для интернационализации)
РУССКАЯ_ПЕРЕМЕННАЯ=значение
GREETING="Привет, мир!"

# Крайние случаи
JUST_EQUALS==
DOUBLE_EQUALS=value==another
STARTS_WITH_QUOTE="value
ENDS_WITH_QUOTE=value"

# Последний комментарий в файле